# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# TensorFlow
*.tflite
# But we want to keep the exported model in assets? 
# Usually, we don't push large models to git, but for this project the user might want it.
# I'll allow .tflite for now since it's the primary output.

# Flutter/Android
.dart_tool/
.packages
.pub-cache/
.pub/
build/
app/android/app/build/
app/android/local.properties
app/android/key.properties
app/android/app/src/main/assets/palm_segmentation*.tflite # Let's ignore them to keep repo small
*.lock

# IDEs
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
